<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Deklaracja tu (nie tylko w :platform-android) potrzebna, żeby własny pakiet testów
         instrumentowanych modułu (`connectedDebugAndroidTest`) mógł faktycznie wysłać SMS. -->
    <uses-permission android:name="android.permission.SEND_SMS" />

</manifest>
